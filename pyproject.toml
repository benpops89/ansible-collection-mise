[project]
name = "ansible-collection-mise"
version = "0.1.0"
description = "Ansible collection for managing mise tools"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ansible-core>=2.20.3",
    "pytest>=9.0.2",
]
